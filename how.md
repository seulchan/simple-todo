1. install nodejs
    - https://nodejs.org/en/download/package-manager/current
2. install Vue CLI
    ```
    npm install -g @vue/cli
    ```
3. install project
    1. git clone 
    1. npm install
    2. npm run serve